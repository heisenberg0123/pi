{% extends 'staffdash/index.html.twig' %}

{% block body %}
    <main id="main" class="main">
        <a href="{{path('adddr')}}" class="btn btn-primary">add</a>
        <table class="table table-striped">
            <thead>
            <tr>

                <th>
                    pret
                </th>
                <th>mois</th>
                <th>Montant paye</th>
                <th>perode retard</th>
                <th>Actions</th>
            </tr>

            </thead>
            <tbody>
            {%for  i in  pret %}
                <tr><th>{{i.rmb}}</th>
                    <th>{{ i.Mois }}</th>
                    <th>{{i.MontantPaye}}</th>
                    <th>{{i.PeriodeRetard}}</th>


                    <th>
                        <a class="btn btn-warning" href="{{path('updateer',{'id':i.id})  }}"  class="btn btn-info">update</a>
                        <a class="btn btn-danger" href="{{path('deleter',{'id':i.id})  }}"  class="btn btn-warning">delete</a></th>
                </tr>

            {% endfor %}

            </tbody>
        </table>
    </main>

{% endblock %}
